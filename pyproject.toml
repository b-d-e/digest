[project]
name = "whatisbenjireading"
version = "0.1.0"
description = "Daily AI-safety/security reading-list digest, generated by Claude with web search."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.69",
    "httpx",
]

[tool.uv]
# Standalone script, not an importable library — don't try to build/install it.
package = false
